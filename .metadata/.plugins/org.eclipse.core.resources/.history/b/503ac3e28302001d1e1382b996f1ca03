package chapter02;

public class RandomNumber {
	public static void main(String[] args) {
		
	}
	// 반환타입은 int이다.
	// 메소드명은 rollDie이다. rollDie 안에 있는 () 것들은 매개변수이다.
	
	public static int rollDie() {
		// random은 double타입이다. (그렇기 때문에 0과 1 사이의 실수를 반환한다.)
		// 변수 x의 값으로 넣는다.
		double x = Math.random();
		return;
	}
}
